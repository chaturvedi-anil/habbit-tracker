<link rel="stylesheet" href="./css/profile.css">
<main>
    <h1>User Profile</h1>
    <ul>
        <% if(locals.user){ %>
            <li>
                <%= user.name %>
            </li>
            <li>
                <%= user.email %>
            </li>
        <% } %>
    </ul>

</main>